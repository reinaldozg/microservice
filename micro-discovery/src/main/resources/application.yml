spring:
   application:
      name: registry
   devtools:
      livereload:
         port: 35731

server:
    port: 8081

eureka:
    client:
        register-with-eureka: false
        fetch-registry: false
        service-url:
            defaultZone: http://localhost:${server.port}/eureka/